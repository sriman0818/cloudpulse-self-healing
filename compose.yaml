services:

  self-healing-engine:
    build: .

    container_name: cloudpulse-engine

    ports:
      - "8000:8000"

    volumes:
      - /var/run/docker.sock:/var/run/docker.sock

    restart: always
